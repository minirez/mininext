/**
 * Default fallback values used across the application
 * Centralizes hardcoded strings to prevent inconsistency
 */

/** Placeholder name used for unknown/empty guest entries (for filtering) */
export const GUEST_PLACEHOLDER_NAME = 'Guest'

/** Default guest first name (Turkish) when no name is available */
export const DEFAULT_GUEST_FIRST_NAME = 'Misafir'

/** Default guest last name when no last name is available */
export const DEFAULT_GUEST_LAST_NAME = '-'

/** Default cashier/operator name */
export const DEFAULT_CASHIER_NAME = 'Kasiyer'

/** Default display name for unknown guests */
export const DEFAULT_GUEST_DISPLAY_NAME = 'Misafir'

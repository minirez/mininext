# ===========================================
# BOOKING ENGINE API - ENVIRONMENT VARIABLES
# ===========================================
# Copy this file to .env and fill in the values
# NEVER commit .env files to version control!

# Environment
NODE_ENV=development

# Server
PORT=4000

# Database
MONGODB_URI=mongodb://localhost:27017/booking-engine

# JWT Authentication
# IMPORTANT: Generate a strong secret for production!
# Run: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_GENERATE_STRONG_SECRET_MIN_64_CHARS
JWT_ACCESS_EXPIRE=15m
JWT_REFRESH_EXPIRE=7d

# CORS (comma-separated list of allowed origins)
CORS_ORIGIN=http://localhost:3000,http://localhost:5180

# AWS SES (Email) - Optional
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SES_REGION=eu-west-1

# AI Translation (Gemini) - Optional
GEMINI_API_KEY=

# Encryption Key - Optional (for sensitive data encryption)
# Run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=

# API Public URL - Used for email logo URLs, file links, etc.
API_URL=http://localhost:4000

# Frontend URL - Used for password reset links, etc.
FRONTEND_URL=http://localhost:5180

# Storefront API Key - For server-to-server auth from storefront frontends (site3 etc.)
# Allows storefront servers to call public Paximum/bedbank endpoints without a user JWT.
STOREFRONT_API_KEY=

# Sentry - Error tracking (optional)
# Get DSN from https://sentry.io → Project Settings → Client Keys (DSN)
SENTRY_DSN=

# Redis - Optional (for distributed rate limiting in multi-instance deployments)
# Set REDIS_ENABLED=true to enable Redis-backed rate limiting
REDIS_ENABLED=false
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_KEY_PREFIX=booking:
